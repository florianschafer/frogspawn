package net.adeptropolis.nephila.graph.implementations;

import net.adeptropolis.nephila.graph.implementations.buffers.MatrixEntryBuffer;
import sun.misc.Unsafe;

import java.util.Comparator;

public class AdjacencyMatrix {

  private static final Unsafe UNSAFE = Unsafe.getUnsafe();


  public void foo(MatrixEntryBuffer buffer) {


//    Int2IntOpenHashMap


//    Comparator<Object> objectComparator = Comparator
//            .comparingLong(idx -> buffer.getRow((Long) idx))
//            .thenComparing(Comparator.comparingLong(idx2 -> buffer.getCol((Long) idx2)));




//    it.unimi.dsi.fastutil.Arrays.mergeSort();
//    long[] indices = LongStream.range(0, buffer.size).toArray();
//    Arrays.sort(indices, Comparator.comp);



  }

}
